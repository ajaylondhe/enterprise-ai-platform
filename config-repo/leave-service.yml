server:
  port: 8083

spring:
  application:
    name: leave-service

  datasource:
    url: jdbc:mysql://localhost:3307/enterprise_ai
    username: enterprise
    password: enterprise

  jpa:
    hibernate:
      ddl-auto: update

eems:
  leave:
    max-days-per-request: 15